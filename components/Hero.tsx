'use client'

import { ShaderBackground, HeroContent } from './ui/shaders-hero-section'

export default function Hero() {
  return (
    <ShaderBackground>
      <HeroContent />
    </ShaderBackground>
  )
}
